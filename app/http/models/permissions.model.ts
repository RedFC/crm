"use strict";

export interface Permissions {
    INVITE          : Boolean,
    REMOVE          : Boolean,
    VIEW            : Boolean,
    PASSWORD_RESET  : Boolean,
    ADD             : Boolean,
    UPLOAD          : Boolean,
    SWAP_ADMIN      : Boolean 
}